# API Dependencies (FastAPI + Logfire)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
logfire>=0.20.0
pydantic>=2.0.0
python-multipart>=0.0.6
aiofiles>=23.0.0

# UI Dependencies (Streamlit)
streamlit>=1.28.0
requests>=2.31.0

# Already in requirements.txt but needed here for API:
# pandas, numpy, scikit-learn, etc. are imported from automl_agent
